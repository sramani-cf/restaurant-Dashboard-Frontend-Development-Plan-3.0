# Server Configuration
NODE_ENV=development
PORT=8000
API_VERSION=v1

# Database Configuration (PostgreSQL)
DATABASE_URL="postgresql://restaurant_user:Avalon@1001@localhost:5432/restaurant_dashboard_db"
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=restaurant_user
POSTGRES_PASSWORD=Avalon@1001
POSTGRES_DB=restaurant_dashboard_db

# Test Database (for running tests)
TEST_DATABASE_URL="postgresql://restaurant_user:Avalon@1001@localhost:5432/restaurant_dashboard_test_db"

# JWT Configuration (REQUIRED)
JWT_SECRET="your-super-secret-jwt-key-at-least-32-characters-long-for-security"
JWT_REFRESH_SECRET="your-super-secret-refresh-jwt-key-different-from-above-and-also-32-chars"
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Redis Configuration (Optional - for caching and sessions)
REDIS_URL=""
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=""

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_SKIP_SUCCESSFUL_REQUESTS=false

# File Upload Configuration
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp,application/pdf

# Cloudinary Configuration (for image/file storage)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Email Configuration (SMTP)
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASSWORD=""
FROM_EMAIL=""
FROM_NAME="Restaurant Dashboard"

# Security Configuration
BCRYPT_SALT_ROUNDS=12
SESSION_SECRET="your-session-secret-key-for-express-sessions"

# WebSocket Configuration
SOCKET_IO_CORS_ORIGIN=http://localhost:3000
WEBSOCKET_PORT=3002

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=""

# Development Tools (remove in production)
# SWAGGER_ENABLED=true